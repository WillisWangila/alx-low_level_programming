#include "main.h"

/**
 * _abs - computes absolute value of an integer
 * @x: integer to be checked
 * Return: Absolute
*/

int _abs(int x)
{
	if (x > 0)
	{
		return (x);
	}
	else
	{
		return (-x);
	}
}
